#pragma once                                                                    // Гарантируем однократное включение заголовка при компиляции
//
#define LGFX_USE_V1                                                              // Сообщаем библиотеке LovyanGFX использовать версию API v1
#include <LovyanGFX.hpp>                                                         // Подключаем основной заголовок LovyanGFX
#include <lvgl.h>                                                                // Подключаем заголовок графической библиотеки LVGL
//
#include "HardwareConfig.h"                                                     // Получаем определения пинов и настроек оборудования
#include "TouchCalibration.h"                                                   // Для доступа к данным калибровки тачскрина
//
class LGFX : public lgfx::LGFX_Device {                                           // Класс-драйвер экрана, наследуется от базового устройства LovyanGFX
  lgfx::Panel_ILI9341 _panel;                                                     // Объект панели конкретного контроллера ILI9341
  lgfx::Bus_SPI       _bus;                                                       // Объект SPI-шины, через которую работает панель
  lgfx::Touch_XPT2046 _touch;                                                     // Объект тачскрина на контроллере XPT2046
//
public:                                                                          // Публичный интерфейс класса
  LGFX();                                                                        // Конструктор, настраивающий все компоненты
};                                                                               // Завершение определения класса
//
extern LGFX tft;                                                                 // Глобальный экземпляр драйвера дисплея, объявление для других файлов
//
lv_display_t* createLvglDisplay();                                               // Функция создания объекта дисплея LVGL
void initPointerInput();                                                         // Функция инициализации устройства ввода (тач)

